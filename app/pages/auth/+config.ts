import { Config } from "vike/types";
import { LayoutWithoutBar as Layout } from "../../layouts/LayoutDefault";
import vikeReact from "vike-react/config";
export default {
    Layout:Layout,
    title: "Login"
} satisfies Config